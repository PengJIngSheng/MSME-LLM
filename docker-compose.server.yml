services:
  mongo:
    ports:
      - "${SERVER_BIND_IP:-127.0.0.1}:${MONGO_PORT:-27017}:27017"

  pgvector:
    ports:
      - "${SERVER_BIND_IP:-127.0.0.1}:${PGVECTOR_PORT:-5432}:5432"

  ollama:
    ports:
      - "${SERVER_BIND_IP:-127.0.0.1}:${OLLAMA_PORT:-11434}:11434"
    gpus: all
